// This is a listener that checks when the website/firebase has loaded, something should happen
// @TODO, will put our ingest here
document.addEventListener("DOMContentLoaded", event => {
    const app = firebase.app();
});